/**
 * APPS SCRIPT REMOVED
 * The system now uses native Google OAuth 2.0 & Google Sheets REST API.
 * All credentials (Client ID, Client Secret, Refresh Token, Spreadsheet ID) are configured in .env or Settings modal.
 */
